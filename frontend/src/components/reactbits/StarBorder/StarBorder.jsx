import { createElement } from 'react';import './StarBorder.css'; export default function StarBorder({as='button',className='',color='cyan',speed='6s',thickness=1,children,...props}){return createElement(as,{className:`star-border ${className}`,style:{'--star-color':color,'--star-speed':speed,padding:thickness},...props},<span>{children}</span>)}
