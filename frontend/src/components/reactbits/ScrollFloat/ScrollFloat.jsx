import { useLayoutEffect, useRef } from 'react';
import gsap from 'gsap'; import { ScrollTrigger } from 'gsap/ScrollTrigger'; import './ScrollFloat.css';
gsap.registerPlugin(ScrollTrigger);
export default function ScrollFloat({children,containerClassName='',textClassName='',animationDuration=1,ease='back.inOut(2)',scrollStart='center bottom+=50%',scrollEnd='bottom bottom-=40%'}){const ref=useRef(null);useLayoutEffect(()=>{if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;const ctx=gsap.context(()=>gsap.fromTo(ref.current.querySelectorAll('.scroll-float-char'),{willChange:'opacity, transform',opacity:0,yPercent:120,scaleY:2.3,scaleX:.7,transformOrigin:'50% 0%'},{duration:animationDuration,ease,opacity:1,yPercent:0,scaleY:1,scaleX:1,stagger:.03,scrollTrigger:{trigger:ref.current,start:scrollStart,end:scrollEnd,scrub:true}}),ref);return()=>ctx.revert()},[animationDuration,ease,scrollStart,scrollEnd]);return <h2 ref={ref} className={`scroll-float ${containerClassName}`}><span className={`scroll-float-text ${textClassName}`}>{String(children).split('').map((c,i)=><span className="scroll-float-char" key={i}>{c===' '?' ':c}</span>)}</span></h2>}